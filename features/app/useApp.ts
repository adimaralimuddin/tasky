import React from "react";
import { useSelector } from "react-redux";
import { RootState } from "../../store";

function useApp() {
  //   const {content} = useSelector((s: RootState) => s.app);
  return {};
}

export default useApp;
