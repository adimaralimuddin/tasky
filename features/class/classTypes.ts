export type ClassCreate = {
  name?: string;
  description?: string;
  userId?: string;
  sample?: boolean;
};

export type ClassType = {
  id: string;
  name?: string;
  description?: string;
  userId?: String;
  sample?: boolean;
};
